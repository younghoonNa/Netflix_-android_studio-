<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mp_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MP_FINAL">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseCharacter" android:label="ChooseCharacter"></activity>
        <activity android:name=".MainProject" android:label="Main Project"></activity>
        <activity android:name=".VoteResult" android:label="Vote Result"></activity>
        <activity android:name=".MovieDes" android:label="Movie Description"></activity>
    </application>

</manifest>